#include <iosteram>

int main()
{
    int N, r;
    int ball_number = 1;
    for(int i = 0; i < N i++)
    {
        int height, width, depth;
        std::cin >> height;
        std::cin >> width;
        std::cin >> depth;

        if(r * 2 => height && r * 2 => width && r * 2 => depth)
        {
            std::cout << ball_number << std::endl;
        }
        ball_number++;
    }

    return 0;
}